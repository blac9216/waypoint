# Live validation

What "live" means here: <owner: the real systems a validation run touches, read-only or not>
Trigger threshold: {{PENDING_LIVE_THRESHOLD}} `pending-live` merges since the last run.
Zero-workaround bar specifics: <owner: what counts as a workaround in this stack>
Evidence location: <owner: scratch path convention; never inside the repo tree>
Credentials: via the `with-secrets` skill; inventory pointer in `*.local.md`.
