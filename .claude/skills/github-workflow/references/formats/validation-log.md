# Validation step log format

Sanitized markdown at `<scratch>/validation-log.md`; placeholders only, never lab
identifiers. One row per step per run.

| Run | Step | Proves | Result (PASS/FAIL/PARTIAL/BLOCKED) | Evidence (path in scratch) | Issue filed |
|---|---|---|---|---|---|
