# Build output (audited #362, see root .gitignore for rationale): npm/vite
# tooling-convention names, safe unanchored - not plausible app-source dirs.
node_modules/
dist/
dev-dist/
*.local
.DS_Store
coverage/

# Playwright (issue #468): local run artifacts and the local browser-binary
# cache. Browser binaries are never committed — `npx playwright install
# chromium` per docs/testing.md's "Running the Playwright suite".
test-results/
playwright-report/
blob-report/
playwright/.cache/
